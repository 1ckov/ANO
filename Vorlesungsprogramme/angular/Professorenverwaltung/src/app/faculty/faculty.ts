/**
 * Domain class Faculty:
 * - No Logic
 * - Only data model definitions
 */
export class Faculty {
  constructor(
    public name:string,
    public shortel: string,
    public building?:string
  ) {
  }
}
